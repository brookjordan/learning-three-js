{
  "license": "MIT",
  "dependencies": {
    "dat.gui": "^0.7.7",
    "three": "^0.125.2"
  },
  "scripts": {
    "clean": "rm -rf three/modules",
    "build:modules": "cp -r node_modules three/modules",
    "build:tsc": "yarn tsc --p tsconfig.json",
    "lint": "prettier \"**/*{.js,.html,.css,.json}\" --write  --loglevel warn && eslint \"**/*.js\"",
    "build": "yarn clean && yarn build:modules && yarn build:tsc && yarn lint"
  },
  "devDependencies": {
    "@types/dat.gui": "^0.7.6",
    "eslint": "^7.28.0",
    "eslint-plugin-import": "^2.23.4",
    "prettier": "^2.3.1",
    "typescript": "^4.3.2"
  }
}
